<% set_meta_tags description: @post_sprit_content,
                og: { image:  image_url(@post.picture) } %>

<% provide(:title, @post.title) %>

  <div class="container share-container">

  <%= render 'shared/sns' %>
  
  <% breadcrumb :posts_show  %>
  <%= breadcrumbs separator: " > " , class: :logo %>

    <div class="d-flex align-items-center p-3 my-3 text-white-50 bg-dark rounded shadow-sm">
      <i class="mr-2 fas fa-pen-alt fa-2x"></i>
      <div class="h1 mb-0 text-white lh-100">
        独学の詳細
      </div>
    </div>

    <%= render 'shared/post_show' %>

    <div class="d-flex align-items-center p-3 my-3 text-white-50 bg-dark rounded shadow-sm">
      <i class="mr-2 fas fa-comments fa-2x"></i>
      <div class="h1 mb-0 text-white lh-100">
        コメントリスト
      </div>
    </div>

    <%= will_paginate @post_comments %>

    <%= render 'shared/post_comment' %>

    <%= will_paginate @post_comments %>

    <% if logged_in? %>

      <div class="d-flex align-items-center p-3 my-3 text-white-50 bg-dark rounded shadow-sm">
        <i class="mr-2 fas fa-comments fa-2x"></i>
        <div class="h1 mb-0 text-white lh-100">
          コメントを投稿する
        </div>
      </div>


      <%= render 'shared/post_comment_form' %>

    <% end %>

    <%= render 'shared/sns' %>

    <div class="option">
      <div class="option__like-on bg-info text-light">
        <i class="fas fa-signal"></i>
        　PV数：<%=  @post.impressionist_count %>
      </div>
    </div>

  </div>